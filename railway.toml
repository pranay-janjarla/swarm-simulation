[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "streamlit run oasis_ui.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
